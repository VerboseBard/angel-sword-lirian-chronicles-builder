# Task 012 execution roles — 2026-09-07

## Goal and authority (DECIDED)
Execute the authorized parent `012-website-patch-update.md` at the scheduled time. Its controls, required reading, preservation rules and verification gates apply. Canonical root is `E:\Chat gpt Codex\Angels sword\Angel Sword Lirian Chronicles Beta 2.5 Online`. Current initial HEAD is `4722f45` and status was clean at 13:57 UTC. Recheck before copying. The coordinator owns canonical state and final report; workers write their own scoped reports. No pushes, live-room mutations or frozen-sibling edits.

## Disjoint worker assignments (DECIDED)
1. **API capture:** owns `.tmp_official_site\patch-2026-09-07\api-attempt-01` under the parent workspace, plus canonical `BETA_2.5_TASKS/reports/012-api-capture-report.md`. Read preflight source workflow, audit the canonical puller, copy its script/package scope into that capture attempt and adapt only there to archive raw HTTP bytes/URLs/times/status/headers/SHA-256, bounded timeout/retry, and consistent version capture. Use the observed current explicit rules version. Skip its Playwright snapshot path because public-site capture is separately assigned. Obtain full existing resource/detail coverage and intervening patch notes; retain every attempt, validate schema/counts/references, diff against canonical baseline, re-read current version and sentinel hashes. Report source instability/access errors promptly. Do not write app candidate or run browsers/tests. Preserve exact fetch evidence and produce a machine-readable change ledger and digest of semantic rules impacts.
2. **Official public-site capture:** owns `.tmp_official_site\patch-2026-09-07\site-attempt-01` plus canonical `BETA_2.5_TASKS/reports/012-site-capture-report.md`. Capture official RPG/manual and public Clio builder/vault/sheet/lore pages, sitemap/navigation, all currently referenced relevant modules/curated data/styles/templates and newly exposed rules/game content. Use normal public HTTP/file capture and available browser skill/tools when rendered navigation is necessary. Record raw bytes, URLs/status/timestamps/headers/hashes and missing coverage. Compare retained July 21 Clio source and August report; identify unannounced fixes and newly opened content. No login bypass, live saves, room actions or unrelated crawler scope. Do not write API attempt/app candidate or run local app tests. Capture the official CCS template as evidence; coordinator handles workbook QA/import into candidate.
3. **Isolation and baseline verification:** owns new sibling `Angel Sword Patch Candidate 2026-09-07` and baseline QA records while preparing it, plus canonical `BETA_2.5_TASKS/reports/012-baseline-verification-report.md`. Inventory actual current tracked/untracked/ignored state and protected source hashes, preserve relevant current WIP, create a local Git worktree/branch based on current canonical state (or copied candidate with explicit provenance), and include needed ignored runtime dependencies without changing canonical files. Keep sibling Workshop layout for read-only core check. Run the parent work order's baseline build/rules/community/minmax/VTT/dice/core/npm-test/static-stage gates serially in this candidate before data changes. Preserve all logs and actual browser coverage; inspect free test ports and keep ownership. No API pull, new data, code fixes or publication. New candidate's staging path must be absent/disposable before emit. After baseline report and hashes, hand candidate write ownership back to coordinator. Code/runtime fixes are not allowed during baseline: report failures as baseline.

## Verification and reports (DECIDED)
Each report records inspected inputs/hashes, executed commands/outcomes and limitations, work narrative including failed attempts, files changed, source/candidate locations, and next step. Worker final responses are <=15-line digests. No worker changes the canonical work log or commits canonical records. Candidate creation may create its own local branch; avoid commits until verified implementation later. Coordinator audits each output with a second worker before final delivery.

## Out of scope (DECIDED)
Unrelated features, paid installs, original/frozen app writes, Workshop synchronization, force Git operations, public deployment, and any claim of full compatibility from unrun/skipped tests.
